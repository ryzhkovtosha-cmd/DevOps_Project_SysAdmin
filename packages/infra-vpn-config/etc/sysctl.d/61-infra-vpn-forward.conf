# Приносится пакетом infra-vpn-config.
# Обычный Linux — это хост: пакет, адресованный не ему, отбрасывается.
# VPN-серверу нужно вести себя как маршрутизатор и пересылать пакеты
# между туннельным и внешним интерфейсом.
net.ipv4.ip_forward = 1
